name: Build and deploy style files
on:
  pull_request_target:
    types:
      - closed
  push:
    branches:
      - "release_5-4"
      - "release_6"
      - "release_7"
      - "release_8"
      - "trunk"
      - "trunk_skin_to_repo"

jobs:
  if_merged:
    if: |
      github.event.pull_request.merged == true ||
      github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      all: ${{ steps.changes.outputs.all }}
    steps:
      - if: ${{ github.event.pull_request.merged == true }}
        run: CI/Style-To-Repo/build-and-deploy.sh
        env:
          GHRUN: "yes"
          PR_NUMBER: ${{ github.event.pull_request.number }}
#        run: CI/Style-To-Repo/build-and-deploy.sh <branch> <commitmsg>
#      - if: ${{ github.event_name == 'push' }}
#        run: CI/Style-To-Repo/build-and-deploy.sh <branch> <commitmsg>


#      - name: Branch
#        run: echo ${GITHUB_REF##*/}
#      - name: Env
#        run: echo ${{ github.event.payload }}
#      - name: Style to Repo
#        run: CI/Style-To-Repo/build-and-deploy.sh ${GITHUB_REF##*/}